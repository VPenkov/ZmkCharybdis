---
include:
  # Left
  - board: nice_nano_v2
    shield: charybdis_left
    keymap: qwerty
    format: bt
    artifact-name: charybdis_qwerty_left
  # Right
  - board: nice_nano_v2
    shield: charybdis_right
    keymap: qwerty
    format: bt
    artifact-name: charybdis_qwerty_right
    # TEMP: Studio-over-USB snippet removed so USB logging can own the CDC
    # console for Enter-key debugging. Restore this line when done.
    # snippet: studio-rpc-usb-uart
  # Firmware Reset
  - board: nice_nano_v2
    shield: settings_reset
    format: reset
    artifact-name: firmware_reset_nano_v2
